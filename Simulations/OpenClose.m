classdef OpenClose < SimulationState
    methods
        function me = OpenClose(step)
            me.step = step;
        end
        function start(me)
        end
        function done = isDone(me)
        end
    end
end